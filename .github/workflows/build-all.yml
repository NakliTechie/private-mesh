name: build-all

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  build-all:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Run build-all
        run: ./scripts/build-all.sh
