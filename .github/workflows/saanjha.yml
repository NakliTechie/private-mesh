name: saanjha

on:
  push:
    branches: [main]
    paths:
      - 'saanjha/**'
      - '.github/workflows/saanjha.yml'
  pull_request:
    paths:
      - 'saanjha/**'
      - '.github/workflows/saanjha.yml'

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Smoke test
        run: ./saanjha/smoke.sh
