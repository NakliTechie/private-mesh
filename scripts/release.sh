#!/usr/bin/env bash
# release.sh — reproducible build + signing pipeline.
# Implementation lands in M9. Placeholder so the script path is stable.
set -euo pipefail

echo "release: not yet implemented (M9)"
echo "  M9 will add: reproducible builds, GPG signing, curl|bash installer script, release artifacts."
exit 0
